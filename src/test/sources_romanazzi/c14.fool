//Subclass inside superclass:
class A (int x) {
        B createB() new B(x, 2);
    }
    class B implements A (int x, int y) {
        int getX() x;
        int getY() y;
    }
    ;
    let
        A a = new A(1);
        B b = a.createB();
    in
        print(b.getY());
//-   "2"