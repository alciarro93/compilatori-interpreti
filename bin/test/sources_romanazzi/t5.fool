//Overriding methods with incompatible types:
class A {
        int f(int x) x;
    }
    class B implements A
    class C implements B {
        bool f(bool x) x;
    };
    let
        C c = new C;
    in
        print(c.f(false));
//-   "type errors"