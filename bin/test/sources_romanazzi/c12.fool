//Function returning an object:
class Numero (int x) {
        int getNum() x;
    };
    let
        Numero uno() new Numero(1);
        Numero num = uno();
    in
        print(num.getNum());
//-   "1"