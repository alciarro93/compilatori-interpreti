//Call method on object returned by method:
class Numero (int x) {
        Numero uno() new Numero(4);
        int due() x;
    };
    let
        Numero num = new Numero(2);
        Numero num2 = num.uno();
    in
        print(num2.due());
//-   "4"